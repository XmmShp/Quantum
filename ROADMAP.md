# Quantum Roadmap

Quantum 的下一阶段按以下顺序推进：

1. 先完成远程插件下发，让 Quantum Platform 与桌面宿主形成安全、可恢复的安装和更新闭环。
2. 再建设移动端，复用经过验证的平台服务与 Web 插件基础设施。

路线图以能力和验收门槛为准，暂不绑定具体日期。只有前一阶段达到退出标准，后一阶段才进入正式开发。

## 当前基础

- 桌面宿主已支持 Windows 与 macOS，可从本地 `Modules` 加载 .NET 和 Web 插件。
- 本地 ZIP 已具备安全预检、依赖解析、用户确认、事务安装、热升级和失败回滚能力。
- Quantum Platform 已具备插件发布、审核、检索、兼容性检查、下载、哈希校验和审计能力。
- Web 插件已运行在隔离 iframe 中，并通过 Host capability 使用导航、资源、日志、EventBus 和受控 .NET RPC。

## 第一阶段：远程插件下发

### R1：市场接入与手动安装

目标：用户可以直接在桌面宿主中发现并安装已审核的插件。

- 为宿主增加可配置的 Quantum Platform 地址、超时、代理和连接状态。
- 提供插件市场页面，支持搜索、标签过滤、详情、版本、发布说明和兼容性展示。
- 根据宿主版本、操作系统、CPU 架构和插件 runtime 过滤不可安装版本。
- 下载包后校验文件大小与 SHA-256，再复用现有 ZIP 预检和事务安装管线。
- 安装前展示版本变化、依赖影响、权限或能力声明，并要求用户确认。
- 明确处理断网、超时、服务端错误、磁盘空间不足、包损坏和安装取消。
- 保留本地 ZIP 安装作为开发、离线和故障恢复入口。

完成标准：

- Windows 和 macOS 均可从市场完成 Web/.NET 插件的检索、下载、确认和安装。
- 未审核、不兼容或哈希不一致的包不会进入 `Modules`。
- 下载或安装中断不会破坏现有插件和当前运行时快照。

### R2：更新闭环

目标：用户能够看见、理解并安全应用插件更新。

- 将本地已安装版本与市场发布版本进行对比，展示“可更新”状态。
- 支持单插件更新和批量更新预检；统一计算强依赖、弱联动和级联影响。
- 更新前展示发布说明、兼容性、包来源、哈希和依赖变化。
- 下载完成后继续复用现有热升级事务，生命周期启动失败时恢复旧文件和旧 runtime。
- 记录安装来源、当前版本、最后检查时间和最近一次更新结果。
- 定义稳定版与预览版发布渠道，版本选择必须确定且可解释，避免客户端意外降级。
- 第一版只提供手动检查和手动确认更新；自动更新在安全与灰度能力完善后再评估。

完成标准：

- 更新失败可恢复到更新前的文件与运行时状态；forward-only 数据库 migration 的限制会被明确提示。
- 批量更新在写入前完成整份依赖快照预检，不产生部分更新。
- 市场不可用时，本地插件仍能启动、禁用、启用、卸载和通过 ZIP 更新。

### R3：可信分发与供应链安全

目标：从“包内容未损坏”提升到“包来源可信、发布可撤回、事件可追踪”。

- 为发布包和发布元数据引入签名，宿主内置并可轮换信任根。
- 签名覆盖插件 id、版本、兼容范围、包哈希和关键 manifest 元数据，防止替换与降级攻击。
- 增加签名密钥吊销、恶意版本下架和紧急阻止清单；已安装版本命中时给出明确处置建议。
- 服务端存储不可变发布物，并支持对象存储/CDN、流式下载、断点续传和下载限流。
- 将审核、发布、撤回、下载和客户端安装结果纳入可查询审计链路，日志不得包含凭据。
- 对 ZIP 解析、manifest、依赖图、签名验证和安装恢复开展模糊测试与故障注入。

完成标准：

- 宿主默认拒绝签名无效、已吊销、被替换或遭降级的远程包。
- 信任根轮换和紧急阻止流程经过演练，且不影响离线启动未被阻止的已安装插件。
- 可从一次安装失败追踪到市场发布、下载校验、预检和运行时切换各阶段。

### R4：发布与运营准备

目标：远程下发具备稳定发布和长期维护能力。

- 建立市场服务部署、备份恢复、数据库迁移、监控告警和容量基线。
- 在 CI 中覆盖市场 Contract、Windows/macOS 客户端集成和端到端安装/回滚场景。
- 提供开发者发布、审核、版本兼容、签名和撤回文档。
- 提供用户侧来源、权限、更新、失败恢复和问题反馈说明。
- 支持按发布渠道和客户端批次灰度，能够暂停、扩大或撤回一次下发。
- 采用可关闭的功能开关逐步放量；遥测遵循最小化、透明和用户可控原则。

阶段退出标准：

- 远程安装、更新、撤回和故障恢复在 Windows 与 macOS 上达到发布质量。
- 市场故障不会阻塞宿主启动或已安装插件的本地使用。
- 安全响应、服务回滚和数据恢复流程均有文档并完成演练。

## 第二阶段：移动端

移动端采用 .NET MAUI Blazor Hybrid 宿主，但只运行 Web 插件，不加载外部 .NET DLL。所有移动端插件都必须经过远程分发的审核、签名、兼容性和阻止清单检查。

### M1：平台验证与契约冻结

目标：在扩大实现范围前验证移动端可行性并固定平台边界。

- 增加 Android 与 iOS 技术验证目标，验证 WebView 隔离、iframe、`postMessage`、本地文件映射和应用生命周期。
- 评估 App Store 与各 Android 渠道对下载后执行 Web 代码、插件审核和热更新的政策要求。
- 在 manifest 中声明支持的平台、最低宿主版本、CPU/设备约束和所需 capabilities。
- 定义移动端 capability 白名单、权限模型和拒绝语义；未声明或平台不支持的能力不可调用。
- 确认桌面与移动共享的 SDK 契约，以及因平台限制而存在的显式差异。

完成标准：

- Android 和 iOS 真机均能稳定安装、激活、停用并清理一个签名 Web 插件。
- 平台政策、安全边界和不支持的能力形成书面决策，阻断项已解决或有明确替代方案。

### M2：移动宿主 MVP

目标：交付可日常使用的移动端基础壳与插件运行时。

- 为 Android/iOS 增加构建、签名、应用标识、权限和 CI 产物。
- 将导航、菜单、设置、安装确认和错误界面适配触控、小屏、安全区、深色模式和无障碍。
- 接入远程市场，仅展示兼容的 Web 插件；下载后写入应用私有目录并执行与桌面一致的校验和事务安装。
- 补齐前后台切换、进程回收、网络变化、低存储、升级恢复和崩溃后重建行为。
- 安全保存市场凭据和用户设置；插件不得直接访问宿主令牌、任意文件或不受控网络。
- 验证资源读取、导航、日志、环境快照和 EventBus 在移动端的生命周期语义。

完成标准：

- 用户可在 Android/iOS 上发现、安装、运行、更新、禁用和卸载 Web 插件。
- 应用离线重启后可运行已安装插件，未完成的下载或安装可以安全恢复或清理。
- 前后台切换和页面反复挂载不会产生重复订阅、残留 iframe 或持续后台任务。

### M3：移动原生能力

目标：以最小权限向插件开放真正有价值的设备能力。

- 按优先级评估并提供分享、文件选择、剪贴板、通知、相机和定位等能力。
- 每项能力同时定义 TypeScript SDK、Host transport、权限提示、取消、错误和清理语义。
- 所有敏感能力均要求 manifest 声明、运行时授权和用户可撤销；默认拒绝后台与静默访问。
- 保持 .NET 与 TypeScript SDK 的公共能力模型一致；若移动运行时无法等价实现，记录差异及原因。
- 为每项能力增加 Android/iOS 真机契约测试和权限拒绝测试。

完成标准：

- 插件只能调用已声明且获用户授权的能力，撤销权限后立即得到可预测的失败结果。
- Android、iOS 与桌面端的共享能力通过同一组契约测试，不存在只有类型声明而没有 Host transport 的能力。

### M4：Beta 与正式发布

目标：完成跨平台稳定性、分发政策和运维准备。

- 通过 TestFlight 和 Android 内测渠道开展分阶段 Beta。
- 覆盖主流系统版本、不同 WebView 版本、弱网、低内存和低存储设备。
- 完成隐私说明、数据删除、权限用途、商店审核材料和安全响应流程。
- 建立崩溃率、启动成功率、插件激活成功率和更新失败率的质量门槛。
- 在移动端质量稳定后，再评估跨设备插件清单同步和用户设置同步。

阶段退出标准：

- Android 与 iOS 均通过商店/渠道审核并达到既定稳定性门槛。
- 远程下发服务故障时，移动宿主仍能离线启动并运行未被阻止的已安装插件。
- 插件安装、权限、生命周期和更新问题可观测、可定位、可恢复。

## 暂不纳入

- 移动端加载外部 .NET DLL、动态程序集或插件自带原生代码。
- 未经审核或签名校验的移动端远程插件源。
- 第一阶段默认静默自动更新插件。
- 在远程下发闭环稳定前同时扩展 Android 与 iOS 的完整产品功能。

## 跨阶段原则

- **本地优先：** 网络和市场是增强能力，不是宿主启动与本地使用的单点依赖。
- **复用事务边界：** 远程来源最终进入同一套预检、依赖解析、确认、写入和运行时切换流程。
- **默认安全：** 远程包先验证来源、完整性、兼容性和能力声明，再接触运行时。
- **可恢复：** 下载、写入和生命周期切换均应可取消、可诊断，并尽可能恢复到上一稳定状态。
- **契约一致：** 修改公共插件能力时同步检查 .NET SDK、TypeScript SDK 及各平台 Host adapter。
- **真机验证：** WebView、权限、生命周期和商店策略相关能力不能只依赖单元测试或桌面模拟。
